安装node
windows用户，需要装msysgit
安装fis3（npm install -g fis3）
下载demo（git clone https://github.com/luckylion0706/demo.git）
安装相关插件（cd fis3-angular-demo  npm install）
bower安装依赖（bower install）
使用FIS编译DEMO（fis3 release）
预览效果（fis3 server  start启动服务器）